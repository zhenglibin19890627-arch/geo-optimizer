"""GEO 优化系统托盘入口（开机自启指向这里）。

用法：
    pythonw tray_app.py    # 静默启动：先拉起后台服务，再显示托盘图标
    托盘菜单：打开界面 / 暂停·启动服务 / 重启服务 / 开机自启 / 退出

说明：
- 退出托盘时会一并停止后台服务；
- 开机自启开关写的是当前用户的注册表 Run 键（无需管理员权限）。
"""

from geo import tray

if __name__ == "__main__":
    tray.main()
