# GEO 优化系统 —— 交付检查报告

> 检查日期：2026-08-13 ｜ 检查范围：缺陷修复状态、可运行性、上轮改动回归、工程卫生、交付物完整性

## 一、结论摘要

| 检查项 | 结论 |
|---|---|
| 遗留缺陷修复状态（#1/#8/#9/#10） | ✅ 全部已在代码中修复 |
| 全量语法检查 | ✅ 通过 |
| 启动冒烟测试（页面/核心接口） | ✅ 通过，无 500 |
| 问题生成器品牌线索改进（上轮改动） | ✅ 回归通过 |
| 工程卫生（git + .gitignore） | ✅ 已补齐（初始提交 c2828aa） |
| 交付物完整性 | ⚠️ 1 项缺口：docs/06_测试结果交接文档.md 缺失 |

## 二、遗留缺陷修复状态（对照 reports/测试执行记录.md）

| 缺陷 | 状态 | 代码位置 |
|---|---|---|
| #1（阻塞）监测线程 SQLite 写锁 | ✅ 已修复 | `geo/core/monitor_task.py:327-329`（调用 AI 前 `s.commit()` 释放写事务） |
| #8（严重）预警写库路径 SQLite 锁 | ✅ 已修复 | `geo/analyzers/alerting.py:125/138/160`（`set_setting` 均传 `session=` 同事务写入） |
| #9（一般）报告页轮次下拉含取消轮 | ✅ 已修复 | `static/js/report.js:74-77`（cancelled → "（已停止）" 标识） |
| #10（一般·建议）中途停止竞态 | ✅ 已修复 | `geo/core/monitor_task.py:354-358`（调用返回后复查取消标志）+ `367-369`（收尾兜底复查） |

## 三、可运行性（冒烟测试，2026-08-13）

- `python -m py_compile` 全量通过（geo/ 全部模块 + app.py）
- `python app.py` 启动正常（GEO_NO_SCHEDULER=1 测试模式）
- 6 个页面全部 HTTP 200：`/`、`/static/{index,monitor,optimize,questions,report,settings}.html`
- 核心 API 返回正常数据（code=0）：`/api/overview`、`/api/monitor/rounds`、`/api/settings`、`/api/brands`、`/api/questions`、`/api/alerts`
- 业务错误按大白话拦截（如空关键词扩展 → "请先填写至少一个关键词，我才能帮你扩展问法"）
- 数据库中存在真实监测数据（round 34，2026-08-12 完成），系统修复后处于持续正常使用状态

## 四、上轮改动回归（问题生成器品牌线索）

- 改动文件：`geo/core/question_expander.py`（品牌线索清单 + 硬性嵌入规则 + expected_trigger 口径）
- 回归结果：生成 5 条问题全部嵌入品牌线索（龙泉/弱电/系统集成/智慧/售后，每条 3-5 条），0 条含品牌名"威启"，GEO 测试原理保持

## 五、工程卫生

- 已初始化 git 仓库（分支 main），初始提交 c2828aa，102 个文件纳入版本控制
- 新增 `.gitignore`：排除 `config/config.yaml`（含 API 钥匙）、`data/`（数据库与备份）、`__pycache__/`、`.reasonix/`、日志临时文件
- 已验证敏感文件零跟踪（git ls-files 中 config.yaml/data/__pycache__/.reasonix 计数为 0）

## 六、交付物完整性

| 交付物 | 状态 |
|---|---|
| README.md（大白话使用说明） | ✅ 存在 |
| config/config.example.yaml（配置模板） | ✅ 存在 |
| requirements.txt | ✅ 存在 |
| data/geo.db（本地数据库，运行时生成） | ✅ 存在（1.0 MB，含真实监测数据） |
| reports/测试执行记录.md（过程证据） | ✅ 存在 |
| docs/06_测试结果交接文档.md（验收结论） | ✅ 已补写（2026-08-13，基于测试执行记录整理，原始交付版缺失——见文首整理说明） |

## 七、待办（需用户决策）

1. ~~**docs/06_测试结果交接文档.md 缺失**~~：已基于 `reports/测试执行记录.md` 补写整理版（`docs/06_测试结果交接文档.md`），文首已如实标注整理性质；如后续拿到测试工程师原始交付版，可替换。
2. ~~**（建议，不阻塞）整理 pytest 套件**~~：已完成（2026-08-13，37 个用例全部通过）。见 `tests/`，运行方式：`pip install -r requirements-dev.txt && python -m pytest tests/`。

## 八、pytest 套件（2026-08-13 新增，同日迭代扩充）

- 位置：`tests/`（conftest.py + 6 个测试文件），依赖声明在 `requirements-dev.txt`（不影响零代码用户的 `requirements.txt`）
- 覆盖：评分（scoring 6 例）、提及/情感/顺位（mention 12 例）、信源解析（sources 7 例）、预警五场景 + 文案（alerting 6 例，用临时库不碰正式数据）、问题扩展器解析与参数校验（expander 9 例，monkeypatch 拦截模型调用零费用）、**API 层回归（api 13 例：Flask test_client + 临时库隔离，覆盖首页引导/品牌与问题库 CRUD/跨品牌隔离/手动粘贴/无钥匙拦截/404/405 状态码/定时设置/钥匙脱敏/优化异步落库/报告趋势信源竞品，零真实 AI 调用）**
- 运行：`pip install -r requirements-dev.txt && python -m pytest tests/` → **53 passed**
- 测试中发现并记录的已知限制：情感规则法词典对"不是很好"判定为 neutral（"很好"被否定反转 -2 与"好"单独计数 +1 抵消），语义上应为负面。**已于 2026-08-13 修复**（`geo/analyzers/mention.py`：词典去重 + 否定回看窗口 2→4 字 + 反转整词记 -1 + 排除"无比/不仅"等非否定短语 + 剔除"不好意思"礼貌短语），新判定逻辑只影响修复后新入库的数据，历史 sentiment 口径保持原样。

## 九、最终一致性检查（2026-08-13）

| 检查项 | 结论 |
|---|---|
| 前端 JS 语法（static/js/*.js 8 个文件，node --check） | ✅ 全部通过 |
| 依赖核对：代码 import 的 7 个第三方库（flask/flask-cors/sqlalchemy/apscheduler/requests/beautifulsoup4/pyyaml）vs requirements.txt | ✅ 一一对应，无缺失无多余 |
| 配置节核对：engines/analysis/fetch/monitor/pricing 均为活配置（代码有读取） | ✅ |
| 配置节核对：**schedule 节为死配置**（无代码读取，实际存数据库 settings 表、默认值硬编码） | ⚠️ 已修正：config.example.yaml 该节加注释说明"以设置页为准，改这里不生效" |
| README 引用的文件路径（config/config.yaml、data/geo.db） | ✅ 均存在 |
| git 工作区 | ✅ 干净（6 个提交） |

## 十、数据库健康与安全面检查（2026-08-13）

| 检查项 | 结论 |
|---|---|
| 数据库表完整性（data/geo.db） | ✅ 12 张表全部存在且与 models 定义一致（测试记录"11 张表"为旧口径，api_call_log 已含在内） |
| 数据量概况 | ✅ 真实使用数据：34 轮监测 / 124 条回答 / 27 份快照 / 22 条预警 / 208 条调用日志 |
| 数据一致性（孤儿数据） | ✅ 0 孤儿（round/task/brand/snapshot/alert 引用全部有效）；17 条 round_id 为空的手动粘贴记录属**设计行为**（paste 通道 round_id=None，独立记录） |
| 问题库重复文本 | ✅ 0 |
| 钥匙泄露面（代码） | ✅ 无任何 print/log 输出 api_key；key 仅用于 Authorization header |
| 钥匙泄露面（运行时） | ✅ /api/settings/keys 返回脱敏格式（sk-4\*\*\*\*1b37），明文钥匙不回显；无钥匙引擎正确返回 configured=false |

## 十一、工程迭代记录（2026-08-13，按优先级批量落地）

| 优先级 | 事项 | 落地内容 |
|---|---|---|
| P0 | 情感判定缺陷 | `geo/analyzers/mention.py`：词典去重（旧版重复词条一次出现计 2 分）+ 否定回看窗口 2→4 字 + 反转整词记 -1 + 排除"无比/不仅/不过"等非否定短语 + 剔除"不好意思"礼貌短语；"不是很好"由误判 neutral 修正为 negative。历史 sentiment 落库口径不变，仅影响新入库数据 |
| P1 | 依赖漂移 | `requirements.txt` 锁定 7 项运行依赖到实测版本（Flask 3.1.3 等）；`requirements-dev.txt` 增加 `-r requirements.txt` 并锁定 pytest==9.1.1 |
| P1 | 死代码/平台耦合 | `geo/seed/seeder.py`：移除 `__file__.rsplit("\\")` Windows 路径拼接与未使用的模板读取函数，`seed_questions()` 空实现契约与 yaml 备用素材保留 |
| P2 | API 层无回归保障 | 新增 `tests/test_api.py`（13 例）+ 修正 `geo/web/__init__.py` 500 处理器补回 HTTP 500 状态码（原返回 200） |
| P3 | 前端大文件 | `static/js/report.js`（1176 行）机械拆分为 `report-trend.js`（401）+ `report-deep.js`（473）+ `report.js`（319），全局函数/状态名不变，加载顺序在 `static/report.html` 声明；Playwright 冒烟（`reports/测试脚本与日志/smoke_report_split.py`）：趋势图渲染、轮次下拉 21 项、信源排行、深度分析卡片、Tab 切换，console/page 零报错，SMOKE_RESULT=PASS |

回归验证：`python -m py_compile` 全量通过；`python -m pytest tests/` → **53 passed**；启动冒烟（GEO_NO_SCHEDULER=1，真实库只读）通过。

## 十二、系统托盘与开机自启（2026-08-14 新增）

| 项 | 内容 |
|---|---|
| 入口 | 新增 `tray_app.py`（`pythonw tray_app.py` 静默启动）+ 核心 `geo/tray.py`；`app.py` 增加 `--no-browser`/`--port` 参数并写 `data/service.json`（端口/pid），`geo/server.py` 抽取端口查找供两入口共用 |
| 托盘菜单 | 打开界面（双击默认）/ 暂停·启动服务 / 重启服务 / 开机自启（勾选态）/ 退出；菜单文案随服务状态动态切换（pystray 0.19.5） |
| 服务生命周期 | 托盘以 pythonw 子进程方式拉起/结束/重启后台服务（日志落 `data/service.log`）；暂停=停掉服务进程（界面与自动监测一并停止，数据保留），重启=结束并重新拉起；服务已在跑时自动"接管"不重复启动 |
| 开机自启 | HKCU `Software\Microsoft\Windows\CurrentVersion\Run` 写入/删除（当前用户级，无需管理员）；命令为 `pythonw.exe "…\tray_app.py"` |
| 单实例锁 | `data/tray.lock` 文件句柄独占锁（CreateFileW 无共享模式）：第二实例得共享冲突自动退出；进程崩溃/被杀时系统自动释放句柄，无陈旧 pid 问题 |
| 依赖 | `requirements.txt` 新增 pystray==0.19.5 / pillow==12.2.0（均已锁定） |
| 验证 | ① `reports/测试脚本与日志/tray_smoke.py` 9 项 PASS（图标生成/启动/探活/HTTP/幂等/暂停/重启/自启开关真实写读删/清理）；② 真实托盘运行：图标循环常驻、服务拉起 HTTP 200；③ `tray_lock_lifecycle_smoke.py` 合并 11 项 PASS（锁 4 路径 + 生命周期 7 项）；④ 全量 pytest 53 passed、py_compile 通过。注册表写入曾受测试沙箱拦截，已在放权窗口用真实键写入-查询-删除验证通过 |

## 十三、监测口径修复：移除品牌背景注入（2026-08-14）

**问题发现**：用户发现监测提及率异常——品牌档案填全后连续 9 轮提及率 100%、评分 89~100，怀疑系统除问题外还给了 AI 额外信息。核查属实：`geo/core/monitor_task.py::build_messages` 把品牌名/别名/产品/简介作为"背景参考"注入系统提示词，并附"可结合实际提及该品牌"的引导语——AI 被提示词诱导点名，100% 提及是提示词工程的产物，并非真实可见度；与"问题里不得出现品牌名"的 GEO 测试原则矛盾（此前 DeepSeek/通义千问真实提及率仅 23%/26%）。

| 项 | 落地内容 |
|---|---|
| 修复 | `build_messages` 改为纯中性：只发"乐于助人的中文AI助手"系统提示词 + 问题本身，品牌档案不再进入任何监测提问（常规/联网模式均生效）；函数签名去掉 brand 参数，调用点同步 |
| 回归防线 | 新增 `tests/test_monitor_task.py` 3 例：只含中性提示词与问题、不含"威启/背景参考/提及该品牌"、提示词无品牌占位；全量 pytest 53 → **56 passed** |
| 历史数据 | 按用户决策**清空全部监测数据**：monitor_result 185 / monitor_round 47 / monitor_task 51 / score_snapshot 37 / alert 27 / competitor_analysis 20 已删除；预警状态键（alert_active_*/alert_warn_at_*）与补跑键一并重置；清空前一致性备份 `data/geo.db.bak-before-clear-20260814-140331` |
| 保留数据 | 品牌档案 4 / 关键词 7 / 问题库 19 / 内容优化记录 2 / 费用日志 271（api_call_log 为成本统计口径，非监测结果） |
| 生效 | 后台服务已重启（新代码），下一轮监测起按中性口径统计；明早 08:30 定时监测将使用新口径。预期提及率回归真实水平（个位数~50%），评分随之回落属正常现象 |

## 十四、报告页单轮数据可见性修复（2026-08-14）

- 问题：`static/js/report-trend.js::loadTrend` 有"轮次 < 2 不画趋势图"的硬门槛——刚清空数据后只跑 1 轮，报告页趋势卡片只显示"再监测 1 轮后…"，看不到本轮数据。
- 修复：门槛放宽为"轮次 < 1 才显示空状态"，1 轮也正常画点（单点带数值标签）；接口无数据时的兜底文案同步改为"暂时没有可展示的趋势数据"。
- 其余板块核对：轮次下拉/本轮引擎明细/信源排行/竞品统计与深度分析/竞品趋势接口本就支持单轮（后端 1 轮路径有 `tests/test_api.py::test_报告接口趋势信源竞品` 覆盖）。
- 测试：该用例改为自包含（不再依赖前序用例创建品牌，单独 `-k` 运行也可过）；全量 pytest 56 passed；`node --check` 通过。

## 十五、新增第 6 家引擎：OpenCode 订阅套餐（2026-08-14）

| 项 | 内容 |
|---|---|
| 适配器 | 新增 `geo/engines/opencode.py`（OpenAI 兼容协议，默认 `https://opencode.ai/zen/v1`，接口地址/模型名可在 config.yaml 或设置页改）；无联网提问能力，联网档自动排除 |
| 注册 | `geo/engines/__init__.py` 登记 + AUTO_CODES 追加 "opencode"；`enabled_auto_engines()` 改为遍历 AUTO_CODES（新增引擎零改动自动纳入"全部"默认勾选） |
| 配置 | `config/config.example.yaml` 新增 engines.opencode 节（钥匙留空、示例模型档）；订阅制计费：不加入 pricing 估算，api_call_log 仍记录 token |
| 界面 | 设置页钥匙卡片/档位下拉/测试按钮、监测中心勾选、报告页显示名均按注册表自动出现；文案"5 家"→"6 家"（index/monitor/common.js/README） |
| 测试 | 新增 `tests/test_engines.py` 4 例（注册齐全/适配器属性/联网排除/未知引擎报错）；钥匙列表断言 6→7；全量 pytest 56 → **60 passed** |
